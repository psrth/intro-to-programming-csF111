#include<stdio.h>

int main () {
	// int x = 10;
    
    // // initialisation
    // int *p = &x;

	// // de-referencing
	// printf("%d", *p);
    
    // int *q = &x; // direct referencing
    // int *q = p; // indirect referencing
	// return 0;

    // // an array of pointers
    // int *pt_arr[3];
    // int arr1[] = {1, 2, 3};
    // int arr2[] = {3, 4, 5};
    // int arr3[] = {5, 6, 7};

    // pt_arr[0] = arr1;
    // pt_arr[1] = arr2;
    // pt_arr[2] = arr3;

    // for (int i = 0; i < 3; i++) {
    //     printf("arr1[%d] => adr: %d, val: %d\n", i, (pt_arr[0] + i), (*(pt_arr[0] + i)));
    //     printf("arr2[%d] => adr: %d, val: %d\n", i, (pt_arr[1] + i), (*(pt_arr[1] + i)));
    //     printf("arr3[%d] => adr: %d, val: %d\n", i, (pt_arr[2] + i), (*(pt_arr[2] + i)));
    //     printf("\n");
    // }

    // int *ptr, i, iA[5]={5,10,15,20,25};
    
    


}
